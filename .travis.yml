language: python

python:

  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"

install:

  - pip install flake8 pytest pytest-cov six

script: 

  - python -B -m pytest -vvv --cov-report=term-missing --cov=datahammer -s
  - python -B -m flake8 --max-line-length=120 *.py tests/*.py
